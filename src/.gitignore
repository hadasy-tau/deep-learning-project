cache/
outputs/parts/
outputs/data/
outputs/listen/
outputs/*.parquet
outputs/build_sessions.csv
# Generated by make_ear_check.py: ~7 MB, almost all base64 audio.  Git keeps
# every version forever and cannot delta-compress base64, so the generator and
# its template are committed instead and the page is rebuilt on demand.
ear_check.html
*.flac
*.m4a
*.wav
*.log
__pycache__/
