# Stage 4 inference over the committees chunk corpus.
# Replaces stage1/inference_openai_whisper_large_v3/requirements.txt, which was
# the VoxKnesset arm-A driver's and went with it.
huggingface_hub>=0.30
requests>=2.31
pandas>=2.0
pyarrow>=15.0
soundfile>=0.12
rapidfuzz>=3.0
numpy>=1.24

# Optional. run_lean.py reads shards with DuckDB when it is importable and falls
# back to Arrow otherwise, at roughly twice the peak memory -- which matters on
# the 17 GB laptop the lean runner was built for.
duckdb>=1.0
